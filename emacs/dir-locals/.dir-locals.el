;;; Directory Local Variables
;;; For more information see (info "(emacs) Directory Variables")

((nil . (
         ; Set a variable.
         (eval . (setq my/example-variable "example value"))
         ; Define a function.
         (eval . (defun my/example-function (example-argument)
                   (print example-argument)))
         )))
